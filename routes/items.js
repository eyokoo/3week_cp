// const express = require("express");
// const router = express.Router();
// const controller = require("../controller/items")


// // GET returns the list of items in my database
// router.get("/items", controller.list); 

// // GET returns the list of items in my database
// router.get("/items/:id", controller.get);

// //POST should call the create function, and add an item to my databse
// router.post("/items", controller.create);

// //PUT should call the update fucntion, and update the item in my database
// router.put("/items/:id", controller.update);

// // DELETE should call the delete function, and delete the item from my database
// router.delete("/items/:id", controller.remove);


// module.exports = router;